# Server-only (never prefix with VITE_)
# Create a free key at https://resend.com — then add both vars in Vercel → Settings → Environment Variables

RESEND_API_KEY=
CONTACT_TO_EMAIL=nguncung65@gmail.com

# Optional. Defaults to Resend onboarding sender (works before a custom domain is verified).
# CONTACT_FROM_EMAIL=Portfolio <onboarding@resend.dev>
